import style from "./Navbar.module.css"

const Navbar = () => {
    return (
        <div className={`${style.nav_container}`}>
            <h1>Navbar</h1>
        </div>
    );
}

export default Navbar;